import 'package:flutter/material.dart';

class PersonalCallMainBody extends StatelessWidget {
  const PersonalCallMainBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
